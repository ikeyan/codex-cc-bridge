; 供給元が新しすぎるパッケージを掴まないための待機日数。dependabot.yml の cooldown と同じ 3 日。
; npm ci にも効く (ロック済みの版でも公開日が判定される) ので、CI 側は --min-release-age=0 で外す。
min-release-age=3
; package.json の allowScripts に載っていない依存の install script を警告でなくエラーにする。
strict-allow-scripts=true
