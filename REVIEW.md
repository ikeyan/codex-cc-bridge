---
# 変更は source 側へ (特定リポジトリ固有のパス・語彙は持ち込まない)
source: https://raw.githubusercontent.com/ikeyan/agent-files/main/REVIEW.md
---

# レビュー観点

差分を行ごとに追わない。各ファイルを現在の結果状態で開き、その変更を見ていない初見読者として、ファイル全体 (先頭コメント含む) の文脈で読む。特に以下を捕まえる。

## インターフェースの使い心地 (最重要)

「正しく使うのが簡単・誤用が困難」(make it easy to use correctly and hard to use incorrectly) になっているか。関数・コンテナイメージ・設定・スクリプトすべてが対象。

- 呼ぶ側が毎回書く前処理・既定値を callee が吸収し、**既定のまま正しく動く**か。
- 誤用を型・既定値・マスク等で**構造的に**防げているか (間違った使い方ができない状態か)。
- **名は体を表しているか**: 名前だけで実体・スコープが分かるか (ロックなら保護対象を名前に含める: `_lock` でなく `_redirection_active_lock`)。
- DRY は前提知識。実装中より、この観点でレビュー時に捕まえる方が効く。

## 冗長性 — 既に他から得られるものは書かない/含めない

ある要素 (コメント・行・依存・設定・言及) が、その場の他の情報から導ける・既に提供されているなら不要。**追加するのは「他から得られない情報」を持つものだけ。** 主な形:

- **コメント**: 書いてよい内容は、対象が自己完結的であるべき単位かどうかで決まる。迷ったら自己完結的でない側・書かない側に倒す。
  - **自己完結的であるべき対象 (関数・クラス・定数・スクリプト・コンテナイメージ・設定ファイル等) の説明**: 書いてよいのは契約のうち他から辿れないもの — 事前条件 (引数・環境変数・事前に必要な副作用。欠くと正しさやセキュリティの前提が崩れるもの)、責務 (単一責任。定数なら値の意味)、事後条件・副作用 (責務の具体形)。語彙はその対象が属するドメイン内に閉じる (ドメイン外の語彙が要るのは単一責任を破っているサイン)。例: `--help` があるスクリプトはそれが契約源なので書かず、無いスクリプトは header に呼び出し契約 (入出力・環境変数・引数) を必要最小限書く。
  - **自己完結的でない対象 (関数内の個別の処理、Edit で書く hunk 等)**: 既定は書かない。書いてよいのは善意の誤修正ガードのみ。その言語・一般的なコマンド動作に精通した読者が、コメント無しだと正しい/必要なコードを「壊れている・冗長・削れる」と誤読し、良かれと思って編集して壊しかねない箇所だけに、「守りたい部分: 必要な理由」の形の最小限 (原則 1 行) を隣接させる。中身は制約・外部仕様そのもの。
    - 書き手は Edit 時、hunk 内を自己完結的にしようとして説明コメントを書きがち。自己完結的にすべき単位はふつう関数やファイル全体であり hunk ではないので、hunk に置かれた説明コメントは冗長として捕まえる。
  - どちらの種類でも通らない典型:
    - コード/名前/処理の言い換え
    - 自明な既定動作の説明
    - ツールで辿れるもの:
      - 消した/移した物の行き先
      - インストールしたもの、定義したものの使用箇所
  - 根拠が非自明なら canon (`ikeyan/canon`) に出典つきで残し、コメントは参照に留める (既存なら出典参照 1 行に置換)。
- **経緯・代案の否定・無いことの宣言**: 試して捨てた代案・失敗の痕跡や「Y でなく Z」「不要」は書かない。制約は書くが、根拠が非自明ならその事実は本文でなく出典つきで canon に残して参照する。
- **冗長な言い回し**: 必要十分条件は散文で説明せず `<=>` で書く等、記号・式で締められる所は締める。
- **他から引き込まれるもの**: 別の同梱物が連れてくるものを重ねて指定しない (例: apt で他パッケージの Depends になっているもの)。ただし明示しないと入らないもの (`--no-install-recommends` 下の Recommends 等) は残す。
- **重複**: 繰り返し同じ値や式、処理を書いている場合で、その繰り返しに短い一つの名前をつけられる場合、共通化する。

## 簡素化 — もう 1 段縮める

「冗長性」が情報の重複を消すのに対し、こちらはコード構造そのものを縮める。PR を出す前、および review で 1 行削った後も「もう 1 段縮められないか」を疑う。

- **削減は連鎖する**: 1 行・1 引数・1 分岐を削ったら、それに依存していた変数・関数がまるごと不要にならないか再確認する (ガード削除 → 当該変数が不要 → 関数を 1 段浅く、の連鎖)。
- **合成キーは関数従属で次元を落とす**: set/map の key に複数要素の組を使うとき、要素間に関数従属があれば自由な次元だけ残す。`(host, port)` が `user` から一意に決まるなら key は `user` だけで等価。
- **冪等操作にガードを付けない**: 集合への追加・削除、map への代入等は冪等。`if k not in s: s.add(k)` は `s.add(k)` と等価で、分岐がそのまま消える。
- **獲得と解放はスコープで物理的に対にする**: 手動の try/finally は獲得と try の隙間の例外で解放が漏れる。言語のスコープ終端フック (context manager / `using` / defer / RAII) に載せ、解放を構造的に必至にする。
- **基盤ツールの失敗はそのまま見せる**: `|| { echo "ERROR…"; exit 1; }` で文言を作り込むと本来のエラーを隠し誤誘導する (node crash を「ファイルが古い」と誤報する類)。失敗を防ぎたいだけなら最小構成にする (生成物の上書きは `gen >/dev/null && gen >file`。temp+mv の atomic 化は git 管理下では過剰)。明示が要るのは前提チェック (道具の不在等) だけ。

## 自然言語の書き方

markdown ファイル・frontend の画面に出る文字列・コメント・文字列リテラル内の自然言語が対象。

- **ハードラップしない**: 段落・箇条書きの各項目は途中で改行せず 1 行で書き、折り返しは表示側 (レンダラ・エディタの soft wrap) に任せる。frontmatter の `description` も折り畳みスカラ (`>`) を使わず 1 行で書く。
- **論理構造をインラインの散文に埋め込まない**: 列挙・手順・階層・分類・対応関係は、その媒体で使える構造 (見出し・箇条書き・番号付きリスト・表・行分け) で表現する。文中の「A、B、C を…」「〜の場合は…、それ以外は…」は構造化の信号。
- **読者を想定して書く**: その文の読者が誰かを決め、その読者に必要な情報量・語彙で書く。例: エラー行の読者は次の一手を求めているので「現象 + 取るべき行動」を 1 行で書き、背景は書かない。
